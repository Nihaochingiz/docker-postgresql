# 🐘 PostgreSQL в Docker - Быстрый старт
```bash
git clone https://github.com/Nihaochingiz/docker-postgresql
cd docker-postgresql
```
## 🚀 Запуск контейнера
```bash
docker-compose up -d
```

## 🔌 Подключение к базе данных

### Через интерактивный терминал psql:
```bash
docker exec -it my-postgres psql -U myuser -d mydatabase
```

### Параметры подключения:
- **Хост:** `localhost`
- **Порт:** `5432`
- **База:** `mydatabase`
- **Пользователь:** `myuser`
- **Пароль:** `mysecretpassword` (или из `.env` файла)

## 📊 Создание таблицы users

В интерактивной оболочке psql выполните:
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 🔍 Проверка и управление

### Проверить созданные таблицы:
```sql
\dt
```

### Посмотреть структуру таблицы:
```sql
\d users
```

### Выйти из psql:
```sql
\q
```

## 📁 Структура проекта
```
.
├── docker-compose.yml    # Конфигурация контейнеров
├── .env                 # Переменные окружения (опционально)
├── init.sql            # SQL-скрипты для инициализации
└── postgres_data/      # Том с данными (создается автоматически)
```

## 🛑 Остановка и очистка

### Остановить контейнер:
```bash
docker-compose down
```

### Остановить и удалить том с данными:
```bash
docker-compose down -v
```

## 📝 Полезные команды

### Проверить статус контейнера:
```bash
docker-compose ps
```

### Просмотр логов:
```bash
docker-compose logs postgres
```

### Зайти в bash контейнера:
```bash
docker exec -it my-postgres bash
```

### Выполнить SQL файл:
```bash
docker exec -it my-postgres psql -U myuser -d mydatabase -f /path/to/file.sql
```

## ⚡ Быстрые команды для работы

| Действие | Команда |
|----------|---------|
| Подключиться к БД | `docker exec -it my-postgres psql -U myuser -d mydatabase` |
| Список таблиц | `\dt` |
| Описание таблицы | `\d имя_таблицы` |
| Выход | `\q` |
| Список БД | `\l` |
| Список пользователей | `\du` |

---

## 🔧 Настройка переменных окружения

Создайте файл `.env` для кастомных настроек:
```env
DB_PASSWORD=ваш_пароль
DB_USER=ваш_пользователь
DB_NAME=ваша_база
```

---

**Готово!** База данных запущена и готова к работе. Для начала работы подключитесь к psql и создайте необходимые таблицы.
